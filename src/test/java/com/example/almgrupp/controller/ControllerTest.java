package com.example.almgrupp.controller;import com.example.almgrupp.StudentList.StudentListGenerator;import com.example.almgrupp.model.Student;import org.junit.jupiter.api.BeforeEach;import org.junit.jupiter.api.Test;import java.util.List;import static org.junit.jupiter.api.Assertions.*;class ControllerTest {    private Student student;    private List<Student> studentList;    private Controller controller;    @BeforeEach    void init(){        controller = new Controller();        student = new Student();        studentList = StudentListGenerator.getStudentList();    }    @Test    void addNewStudent() {        controller.addNewStudent(5L, "Erik", "1234");        assertEquals(5, controller.getStudents().size());    }    @Test    void getAllStudents() {        List<Student> testList = controller.getAllStudents();        assertEquals(4, controller.getAllStudents().size());        assertEquals(testList.get(1), controller.getAllStudents().get(1));    }    @Test    void getStudentByName() {    }    @Test    void getStudentByNumber() {    }    @Test    void deleteById() {    }}